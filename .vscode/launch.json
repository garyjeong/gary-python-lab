{
    "configurations": [
        {
            "console": "integratedTerminal",
            "name": "Python: Current File",
            "program": "${file}",
            "request": "launch",
            "type": "python"
        },
        {
            "args": [
                "${input:file_path}"
            ],
            "console": "integratedTerminal",
            "name": "Python:Video",
            "program": "${workspaceFolder}/video/video-to-black.py",
            "request": "launch",
            "type": "python"
        }
    ],
    "inputs": [
        {
            "default": "local",
            "description": "경로를 입력해주세요.",
            "id": "file_path",
            "options": [
                "${workspaceFolder}/video/temp_folder/IMG_7772.MOV",
                "${workspaceFolder}/video/temp_folder/IMG_7773.MOV"
            ],
            "type": "pickString"
        }
    ],
    "version": "0.2.0"
}